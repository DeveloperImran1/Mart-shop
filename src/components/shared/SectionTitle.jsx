
const SectionTitle = ({ title }) => {

    return (
        <div className="">
            <h1 className="text-[48px] font-semibold leading-[56px] ">{title}</h1>
        </div>
    );
};

export default SectionTitle;

